import EmptyState from "../components/EmptyState.jsx";

export default function SiteSettings() {
  return (
    <div>
      <EmptyState
        title="Site content settings coming here"
        description="Hero headline copy, hero image, announcement ticker text, and other editable storefront content."
      />
    </div>
  );
}
